# نسخة لحظة النظيفة لـGitHub

هذه النسخة مهيأة للحفظ في مستودع GitHub خاص. صور الواجهة وملف APK موجودة محلياً داخل `client/public/`، لذلك لا تتضمن واجهة العميل روابط إلى منصة الاستضافة السابقة.

| المسار | المحتوى |
|---|---|
| `client/public/assets/` | صور واجهة لحظة المتبدلة. |
| `client/public/download/` | ملف APK Release 1.8.0 للتنزيل العام. |
| `client/public/app-update.json` | مصدر تنبيه تحديث التطبيق؛ يستخدم رابطاً محلياً من نطاقك. |
| `railway.toml` | أوامر بناء وتشغيل الخادم على Railway. |

## قبل النشر

ارفع محتويات هذا المجلد إلى مستودع GitHub **خاص**. عند اختيار نطاقك أو رابط Railway، سيعمل زر تنزيل APK وتنبيه التحديث من نطاق ذلك الموقع تلقائياً. لا تضع أي ملف `.env` أو مفتاح توقيع Android (`.jks`) أو ملف `keystore.properties` في GitHub.

> لا تستطيع ملفات المشروع التحكم في الشارة التي قد تضيفها منصة الاستضافة خارج كود الموقع. أما هذه النسخة فلا تحتوي في واجهة العميل على أي تسمية أو شعار أو رابط ترويجي للمنصة السابقة.
