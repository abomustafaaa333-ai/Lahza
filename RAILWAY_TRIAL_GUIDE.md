# تجربة Railway لمدة شهر — لحظة

يُجرى النقل كتجربة موازية فقط. تبقى النسخة الحالية هي خدمة العملاء إلى أن تنجح اختبارات الطلبات والإدارة وقاعدة البيانات على رابط Railway مستقل.

## ما جُهّز في المشروع

يحتوي الجذر على `railway.toml` بأمر البناء `pnpm run build` وأمر التشغيل `pnpm start`. تم التحقق محلياً من أن البناء ينشئ خادم Node في `dist/index.js` والواجهة في `dist/public/index.html`. يقرأ الخادم المنفذ من المتغير `PORT` الذي يوفره Railway.

تمت إضافة طبقة خرائط تدعم `GOOGLE_MAPS_API_KEY`. تعمل النسخة الحالية من دون هذا المفتاح عبر الوسيط المدمج، بينما تحتاج نسخة Railway إلى مفتاح Google Maps مباشر لتستمر ميزة حساب مسافة الطريق ورسوم التوصيل.

| المتغير | القيمة في Railway | الغرض |
|---|---|---|
| `NODE_ENV` | `production` | تشغيل الخادم بوضع الإنتاج. |
| `DATABASE_URL` | `${{MySQL.MYSQL_URL}}` | ربط خدمة التطبيق بقاعدة MySQL في المشروع نفسه. |
| `JWT_SECRET` | قيمة عشوائية طويلة | حماية جلسات المالك والمشرف. |
| `GOOGLE_MAPS_API_KEY` | مفتاح Google Cloud خاص بك | حساب مسافة الطريق من مركز منبج. |

## خطوات الحساب والنشر التجريبي

1. أنشئ حساب Railway واربطه بحساب GitHub.
2. ضع كود المشروع في مستودع GitHub **خاص**؛ لا ترفع ملفات مفاتيح APK أو ملفات `.env` أو كلمات مرور قاعدة البيانات.
3. أنشئ مشروع Railway جديداً وأضف خدمة **MySQL**. تدعم Railway ربط التطبيق بخدمة MySQL عبر المتغير `MYSQL_URL`. [1]
4. أضف خدمة جديدة من مستودع GitHub، واترك Railway يقرأ `railway.toml`.
5. أضف متغيرات البيئة السابقة من تبويب Variables، ثم أنشئ نطاق Railway مؤقتاً للخدمة.
6. لا تغير رابط APK ولا تنقل العملاء الآن. اختبر رابط Railway مع طلب تجريبي، وجرّب دخول المالك، وإضافة منتج، وحساب الفاتورة، ثم تحقق من بقاء الطلب في قاعدة البيانات.

## متطلبات قبل التحويل النهائي

يتطلب التشغيل المستقل تماماً نقل صور الواجهة وملف APK من التخزين الحالي إلى تخزين عام مستقل، وإنشاء نسخة احتياطية واستيراد منظم لبيانات MySQL. لا تضع `GOOGLE_MAPS_API_KEY` أو `JWT_SECRET` في GitHub؛ أضفها فقط في متغيرات Railway السرية.

> لا تستخدم النسخة التجريبية لاستقبال طلبات العملاء قبل التحقق اليدوي. في مرحلة التجربة، اجعل الرابط الحالي هو الرابط الرسمي والرابط الجديد للاختبار فقط.

### المراجع
[1]: https://docs.railway.com/databases/mysql "Railway Docs — MySQL"
[2]: https://docs.railway.com/guides/express "Railway Docs — نشر Express من GitHub"
[3]: https://docs.railway.com/config-as-code "Railway Docs — railway.toml"
