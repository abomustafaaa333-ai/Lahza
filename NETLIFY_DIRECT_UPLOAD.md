# الرفع المباشر إلى Netlify — لحظة 1.8.0

استخدم الملف `Lahza-netlify-static-v1.8.0.zip` الذي سُلّم مع الإصدار. لا تفك ضغطه ولا تضعه داخل مجلد إضافي؛ يحتوي الأرشيف على `index.html` و`_redirects` في الجذر مباشرة.

1. افتح لوحة Netlify واختر **Add new site** ثم **Deploy manually**.
2. اسحب ملف `Lahza-netlify-static-v1.8.0.zip` إلى منطقة الرفع وانتظر اكتمال النشر.
3. افتح الرابط العام للموقع ثم افتح المسار `/download` مباشرة. يجب أن تظهر صفحة تحميل لحظة بدلاً من خطأ 404.
4. عند أي تعديل واجهة لاحق، نفّذ `pnpm run build:netlify` من جذر المشروع، ثم اضغط محتويات `client/dist` نفسها في أرشيف جديد وارفعه بالطريقة نفسها.

> هذه الحزمة تنشر الواجهة الثابتة فقط. عمليات الطلب، لوحة الإدارة، التسعير والبيانات ما زالت تحتاج خادم API وقاعدة بيانات عامة؛ لذلك لا يُحوّل رابط تطبيق Android إلى Netlify قبل تجهيزهما.
