# أرشيف كود لحظة

يحتوي أرشيف الكود على مشروع الويب في مجلد `lahza/` ومشروع Android في مجلد `lahza-apk/`، بما في ذلك الواجهة والخادم ومخطط قاعدة البيانات والاختبارات وإعدادات Railway.

لا يحتوي الأرشيف على كلمات مرور أو ملفات بيئة فعلية أو قاعدة بيانات الطلبات أو ملفات البناء المؤقتة. كما لا يحتوي على ملف مفتاح توقيع Android (`.jks`) ولا ملف `keystore.properties` لأنهما سريان. يجب حفظ مفتاح التوقيع الحالي في مكان آمن؛ فهو مطلوب لنشر تحديث APK يثبت فوق النسخة الحالية.

## الملفات المهمة

| المسار | الغرض |
|---|---|
| `lahza/client/` | واجهة العميل ولوحة الإدارة وصفحة تحميل APK. |
| `lahza/server/` | خادم الطلبات وAPI وإدارة الجلسات والحسابات. |
| `lahza/drizzle/` | تعريف جداول قاعدة بيانات MySQL. |
| `lahza/railway.toml` | إعداد نشر الخادم على Railway. |
| `lahza/RAILWAY_TRIAL_GUIDE.md` | خطوات تشغيل تجربة Railway. |
| `lahza-apk/app/` | مشروع Android الذي ينتج ملف APK. |

> لا تنشر الأرشيف أو محتواه في مستودع عام قبل مراجعة الإعدادات وملفات الأسرار.
