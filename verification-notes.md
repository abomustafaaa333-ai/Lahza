# ملخص التحقق

تم التحقق من فتح شاشة التوصيل، واختيار منتج من البقوليات، وإضافته إلى السلة، ثم فتح ملخص الدفع. كما تم التحقق من النقر المزدوج على الشعار، والدخول برمز المالك الافتراضي، وظهور لوحة التحكم وعرض قوائم الطلبات وإدارة الأسعار.

كما تم اختبار عرض الهاتف عبر لقطة محلية بعرض 375 بكسل وظهر التخطيط ضمن الشاشة بصورة سليمة. وتم التحقق من أن نموذج التاكسي يمنع المتابعة بلا موقع انطلاق ووجهة، ثم يفتح تأكيد الرحلة عند استكمالهما. نجح كل من `pnpm check` و`pnpm test` و`pnpm build`؛ وقد مرّت أربعة اختبارات وحدات واكتمل بناء الإنتاج بنجاح.

في التحديث الأخير، تم التحقق بصرياً من خفض عناصر الهيدر وظهور شريط صور الأقسام المتبدل على الهاتف وسطح المكتب، ومن بقاء نموذج إضافة الأصناف بخلفية بيضاء. كما جُرّب تدفق الطلب حتى شاشة «المتابعة كضيف»، التي تطلب الاسم والموقع فقط ولا تعرض رقم الهاتف. نجح `pnpm check` و`pnpm test` بخمسة اختبارات، ونجح بناء الإنتاج وبناء APK تجريبي بإصدار `1.1.0-test` وتحقق توقيع APK عبر مخطط v2.

تم التحقق في تحديث البداية من ظهور شاشة إلزامية عند فتح التطبيق لاختيار «إنشاء حساب» أو «الدخول كضيف»، ومن انخفاض محتوى الهيدر بوضوح على شاشة هاتف. نجح `pnpm check` و`pnpm test` و`pnpm build`، كما تم بناء APK بإصدار `1.2.0-test` والتحقق من توقيع v2.

اختبار تفاعلي: زر «الدخول كضيف» أغلق شاشة البداية وأظهر الخدمات، بينما زر «إنشاء حساب» فتح نموذجاً يضم الاسم ورقم الهاتف الاختياري والموقع وزر تحديد الموقع. لا يمكن إغلاق شاشة البداية من دون اختيار أحد المسارين.

بدأ فحص لوحة المالك بعد إضافة الحضور والتحقق الإلزامي من GPS؛ وسيُستكمل التحقق المباشر من واجهة المتصفح بعد استقرار جلسة المعاينة.

تم التحقق من شاشة البداية بعد تحديث الحضور: الدخول كضيف يفتح واجهة الخدمات بنجاح، وتبقى بيانات الحضور مرتبطة بمعرف جهاز محلي وتُحدّث عند فتح الواجهة أو تفاعل المستخدم.

تم التحقق بصرياً من فتح نافذة دخول الإدارة بالنقر المزدوج على شعار «لحظة»؛ وتظهر النافذة بخلفية بيضاء صريحة وواضحة فوق الخلفية المعتمة للواجهة.

تم التحقق من TypeScript وبناء الإنتاج وسبعة اختبارات وحدات. كما نجح بناء APK `1.3.1-test` وتوقيع v2. أظهر فحص قاعدة البيانات وصول نبضات حضور فعلية من فتح التطبيق، بينما يبقى سجل العملاء فارغاً حتى يسجل أول عميل بعد تحديد موقع GPS عبر الزر.

أُضيف فرض خادمي لرابط خرائط صالح عند إنشاء أي طلب، إضافة إلى تحقق الواجهة. اختبار الوحدة يرفض طلب الضيف بلا رابط GPS، ويرفض تسجيل الحساب بلا رابط GPS، بينما يقبل رابط الخرائط الصحيح. كما نجح فحص جلسة مالك موقعة في قراءة بيانات لوحة العملاء وإرجاع صيغة صالحة للعداد والقائمة؛ وكانت القيم الفعلية وقت الفحص: صفر عميل نشط وصفر حساب مسجل.

أُعيد فتح التطبيق واختيار الدخول كضيف بنجاح بعد تطبيق فرض GPS؛ بدأ التحقق التفاعلي لمسار الطلب من دون موقع موثّق.

أثناء الاختبار التفاعلي لمسار الضيف، فُتحت خدمة التوصيل وقسم الصيدليات بنجاح تمهيداً لإضافة طلب واختبار منع التأكيد قبل تحديد الموقع.

أُضيف طلب صيدلية تجريبي إلى سلة الضيف بنجاح، وتجري الآن متابعة الاختبار إلى صفحة التأكيد من دون تحديد موقع GPS.

وصل طلب الضيف إلى صفحة التأكيد من دون موقع GPS؛ تظهر خيارات إنشاء الحساب أو المتابعة كضيف وزر تأكيد الإرسال، تمهيداً لاختبار رسالة المنع.

في اختبار تفاعلي كامل لمسار الضيف، أُدخل الاسم ثم ضُغط «تأكيد وإرسال الطلب» من دون GPS. بقيت الصفحة على شاشة التأكيد مع ظهور تنبيه «اضغط تحديد موقعي لتأكيد طلبك»، ولم يُرسل الطلب.

تم فتح نافذة دخول المالك تفاعلياً من شاشة التأكيد بالنقر المزدوج على الشعار، ويجري استكمال الدخول للتحقق من قسم العملاء.

تم تسجيل دخول المالك بنجاح؛ ظهر تبويب «العملاء» ضمن قائمة لوحة التحكم، وهو غير معروض في صلاحيات المشرف.

تم فتح تبويب العملاء تفاعلياً للمالك: ظهر عدّاد متواجد واحد ضمن نافذة نشاط مدتها دقيقتان، وقائمة الحسابات فارغة حتى تسجيل أول عميل. ثم تمت العودة إلى واجهة العميل لبدء اختبار حفظ الحساب بلا GPS.

اختبار تفاعلي لشاشة الحساب: بعد إدخال الاسم والضغط على «حفظ والدخول للتطبيق» من دون GPS، ظهرت الرسالة «اضغط تحديد موقعي أولاً ثم احفظ الحساب» وبقيت شاشة الحساب مفتوحة. يكتمل بذلك تحقق الواجهة من منع الحفظ أو إرسال الطلب قبل اختيار الموقع عبر الزر.

اكتمل البناء النهائي بعد فرض رابط GPS خادمياً؛ نجح `pnpm check` و`pnpm test` بثمانية اختبارات و`pnpm build`. كما بني APK `1.3.2-test` وتحقق توقيعه باستخدام مخطط Android APK v2.

في تحديث الطلب المباشر، فُتحت الواجهة من دون أي شاشة تسجيل حساب، وظهر شريط «حقق ١٠ طلبات واربح معنا هدية» المتحرك، ثم فُتحت خدمة التوصيل مباشرة بنجاح.

ضمن اختبار تدفق الطلب المباشر، فُتح قسم الصيدليات وأُدخل عنصر تجريبي تمهيداً للوصول إلى صفحة التأكيد والتحقق من حقول الهاتف السوري والموقع.

أُضيف العنصر إلى السلة وفتحت صفحة التأكيد. ظهرت حقول الاسم ورقم الهاتف مع بادئة ثابتة `+963` وإدخال يبدأ من 9، وزر تحديد الموقع فقط، من دون شاشة إنشاء حساب أو أزرار واتساب والخريطة للعميل.

داخل لوحة المالك، ظهرت روابط الاتصال وواتساب في بطاقات الطلبات الواردة، بما يؤكد بقاء أدوات تواصل الإدارة داخل اللوحة وعدم إتاحتها في واجهة العميل.

في تحديث المشاركة الإدارية، ظهرت أزرار «جهة اتصال» و«مشاركة الموقع» و«صورة الطلب» في بطاقة الطلب للمالك. ظهر زر الموقع مفعّلاً للطلب الذي يحوي رابط GPS، وموجوداً بحالة معطلة واضحة للطلبات القديمة بلا رابط صالح. جُرّب زر صورة الطلب بنجاح وظهرت رسالة «تم تنزيل صورة الطلب» كبديل المتصفح لورقة المشاركة.

بعد إعادة تشغيل المعاينة، جُرّب زر «جهة اتصال» لطلب ذي رقم سوري صحيح، ونجح في توليد بطاقة اتصال للتنزيل؛ وفي APK يفتح الزر نفسه ورقة المشاركة كي يُختار واتساب لإرسال البطاقة كجهة اتصال.

في تحديث التسعير والموظفين، ظهرت تبويبات «رسوم التوصيل» و«موظفو لحظة» للمالك في لوحة الإدارة، وظهرت قائمة اختيار موظف داخل كل بطاقة طلب. كما فُتحت واجهة موظفي لحظة وتحققت حقول الاسم ورقم الهاتف بصيغة +963 وحالة القائمة الفارغة من دون إدخال بيانات تجريبية.

تحقق إعداد رسوم التوصيل من عرض السعر الافتراضي 2 ل.س لكل كيلومتر وإحداثيات مركز منبج 36.5281، 37.9549. نجح استدعاء مسافة طريق حي إلى موقع تجريبي: 37.3 كم، فوترة 38 كم، ورسوم توصيل 76 ل.س، من دون إنشاء طلب جديد.

بدأ التحقق من تجربة العميل من الصفحة الرئيسية حتى خدمة التوصيل، من دون إرسال طلب أو تغيير بيانات قائمة.

فُتح قسم الصيدليات وأُدخل عنصر مؤقت محلياً في نموذج الإضافة تمهيداً لعرض السلة والفاتورة، من دون تأكيد الطلب أو حفظه في قاعدة البيانات.

أُضيف العنصر المؤقت إلى السلة محلياً، ثم جرى بدء الانتقال إلى شاشة المتابعة لفحص فاتورة التوصيل من دون الضغط على تأكيد وإرسال الطلب.

ظهرت شاشة تأكيد الطلب وفيها زر «الاستعلام عن الفاتورة» ورسالة تطلب تحديد موقع العميل قبل إظهار رسوم التوصيل، مع بند إجمالي المنتجات المبدئي. لم يُرسل الطلب أثناء التحقق.

في معاينة غير مؤثرة، تم تحديد موقع GPS مؤقت بنجاح داخل شاشة الفاتورة، وبدأ النظام استدعاء حساب مسافة الطريق تلقائياً. لم يُدخل اسم أو رقم هاتف ولم يُرسل طلب.

في تحديث إدارة الكتالوج، ظهرت تسمية «الكازيات والغاز» في واجهة العميل مع وصف «وقود بالليتر أو قناني غاز»، مؤكدةً انعكاس التسمية الجديدة في أقسام الطلبات.

أثناء التحقق تبيّن تعارض في رمز قنينة الغاز مع رمز البنزين، فتم تصحيحه إلى رمز فريد وإعادة تحميل الكتالوج لإضافة الصنف المقصود بأمان.

بعد التحديث، ظهرت «قنينة غاز» ضمن خيارات قسم الكازيات والغاز. عند اختيارها تحولت خانة الكمية إلى «(قنينة)»، بما يؤكد أن العميل يطلبها بعدد القناني لا بالليترات.

فُتح قسم الأسعار في لوحة المالك للتحقق من واجهة إدارة المنتجات الموسعة، وظهر مؤشر تحميل قائمة المنتجات قبل إظهار حقول الإدارة.

بعد اكتمال التحميل، ظهرت بطاقة «إضافة صنف جديد» بحقول الاسم والقسم ووحدة البيع والسعر، كما ظهرت في كل منتج حقول التعديل مع أزرار الحفظ وإيقاف/تفعيل الإتاحة والحذف.

تم التحقق من رابط ملف APK الموقّع عبر النطاق العام؛ أعاد الرابط تحويل تنزيل مشروع ثم استجابة ناجحة 200 للملف، ما يؤكد أن ملف التطبيق متاح للتحميل.

تأكدت النسخة المنشورة من ظهور عبارة التصميم الجديدة في الصفحة الرئيسية. في المقابل، أعاد المسار العام `/download` صفحة 404 رغم ظهور الصفحة في المعاينة، لذلك بقي التحقق من رابط صفحة التنزيل قيد المعالجة قبل التسليم.

بعد اكتمال النشر، فُتحت صفحة `/download` بنجاح على النطاق العام، وظهر زر تحميل APK ورابطه إلى ملف الإصدار 1.7.0 الموقّع. تم اختبار تفعيل زر التنزيل من الصفحة العامة.
